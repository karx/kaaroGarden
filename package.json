{
  "name": "ebrain-gardener",
  "version": "0.1.0",
  "description": "PROCESS → TRIAGE → PUBLISH pipeline for the eBrain Digital Garden vault",
  "main": "main.js",
  "scripts": {
    "dev": "node esbuild.config.mjs",
    "build": "node esbuild.config.mjs production",
    "postbuild": "node scripts/deploy.mjs"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "builtin-modules": "^3.3.0",
    "esbuild": "^0.19.0",
    "obsidian": "latest",
    "tslib": "2.6.0",
    "typescript": "5.3.3"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
